# The forecast model image. It is built in the flow_predictions repository
# (serving/Dockerfile, tagged flow-serving), so build that first:
#   docker build --platform linux/amd64 -f serving/Dockerfile -t flow-serving .
FROM flow-serving:latest
